import 'package:flutter/material.dart';

class GridListTopSeller extends StatelessWidget {
  const GridListTopSeller({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
